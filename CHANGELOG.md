# 変更履歴

版の付け方は [Semantic Versioning](https://semver.org/lang/ja/) に従う。

---

## 未リリース

**1.0 に向けた棚卸し（`docs/design-1.0.md`）を片付けた版。**
「書いてあるとおりに動いていないもの」6件、「1.0 の前にしか直せない型」4件、
**設定キーの揃え直し**、そして `jimble-mcp` を約束の外に出しました。

> [!WARN]
> **設定ファイルは書き直しが要ります。**
> 時間と大きさは**単位を値に書く**形になり（`30m` / `10MiB`）、
> **素の数値は起動時に落ちます**。落ちるときはキー名と直し方が出ます——
> 黙って既定値に倒れることはありません。
> 全部の一覧は [設定](https://jimble.io/ja/config) にあります。

### 上げる前に見るところ

| | |
|---|---|
| **時間と大きさは、単位を値に書きます**（D-159） | `session.timeout = 30m` / `upload.max_file_size = 10MiB` の形です。**素の数値は起動時に落ちます。** 単位を名前に書いていたときは、`assets.max_age = 3600000` が**そのまま通って 41 日のキャッシュ**になりました。逆にミリ秒の欄に秒を書けば 1000 分の1です。**どちらも例外もログも出ません。** `MB` は 1000 の3乗、`MiB` は 1024 の3乗（HOCON の決まり）なので、既定値と比べるときは別のものだと思ってください |
| **単位を名前に書いていたキーは、名前が変わりました**（D-159） | `session.timeout_minutes` → `session.timeout = 30m`、`sse.retry_millis` → `sse.retry = 3s`、`auth.lockout.forget_hours` → `auth.lockout.forget = 24h` のように、**末尾の単位を落としました**（29 本）。`assets.max_age` のように元から無単位だったものは**名前はそのまま、値に単位が要ります**。対応表は [設定キー一覧](https://jimble.io/ja/config#設定キー一覧) に |
| **`db.<名前>` の綴りが snake_case に揃いました**（D-159） | `maximumPoolSize` → `maximum_pool_size`、`idleTimeout` → `idle_timeout`、そして **`scheme` → `schema`**（綴り違いでした）。**古い綴りもまだ読めますが、起動時に1度だけ警告が出ます。** あわせて、**この表に無いキーを書くと起動時に落ちます**——以前は `hasPath` で1つずつ拾っていたので、**綴りを間違えたキーは何も言わずに無視され**、`maximum_pool_size` を打ち間違えたアプリは**プールが既定のまま**でした |
| **`cipher.*` を設定しているなら、`hash.password.encrypt` も書いてください**（D-159） | 書いていないと**起動時に落ちます**。既定は「`cipher.key` と `cipher.iv` の**両方**が揃っていれば true」でしたが、Javadoc も CHANGELOG も要件も**「`cipher.key` があれば true」**と書いていました。つまり **`cipher.key` だけ足したアプリは false のまま**で、**あとから `cipher.iv` を足した瞬間に反転**し、保存済みの BCrypt が「暗号化済み」として読まれて**全員ログインできなくなります**。`cipher.*` をまったく書いていないアプリは、これまでどおり何も書かなくて構いません |
| **`upload.max_total_size` の既定が 10MiB になりました**（D-159） | 以前は 50MiB でしたが、`server.max_request_size`（10MiB）に頭を押さえられて**決して届きませんでした**。しかも出るのはアップロードのエラーではなく別の失敗です。**いまは超えていると起動時に落ちます**——大きいものを受けるなら、**両方**上げてください |
| **CSRF トークンの寿命が `csrf.max_age`（既定1日）になりました**（D-159） | 以前は `cookie.max_age`（既定1年）に相乗りしていたので、**アプリが `cookie.max_age` を短くすると CSRF も一緒に短くなり**、出るのは「CSRF トークンがありません」の 403 だけでした |
| **`per=all` に上限が付きました**（D-159） | `paging.max_per`（既定 200）です。以前は上限が無く、**誰でも `?per=all` と打つだけで全件を引けました**。本当に全件が要るなら `paging.max_per = 0` にしてください。あわせて設定キー名を **`paging.name_page` / `paging.name_per`** に改めました——`paging.per` は**リクエストパラメータの名前**を入れる欄なのに件数に見え、`paging.per = 50` と書くと**パラメータ名が `"50"` になって** `?per=20` が読まれなくなっていました |
| **`io.jimble.mcp.*` は 1.0 の約束の対象外になりました**（D-158） | [MCP](https://jimble.io/ja/mcp) は2年で5版が出ており、毎回破壊的な変更が入ります。jimble は1版だけ実装するので、仕様が動くたびに**「公開 API を壊す」か「仕様に追随しない」かの二択**になり、**「壊す前に非推奨期間を1マイナー置く」と同居できません**。あわせて **`McpProtocol.VERSION` を `version()` にしました**——`public static final String` は**アプリのバイトコードへ焼き付く**ので、定数のままだと jimble を上げても**古い版の文字列を持ち続け、警告も出ません** |
| **`new Principal(...)` と書いていませんか**（D-157） | `Principal` が record ではなくなりました。**`Principal.of(...)` に書き換えてください**（引数は同じです）。`id()` / `name()` / `role()` / `hasRole()` / `ANONYMOUS` はそのままです。record をやめたのは、**あとからテナントIDや複数役割を足せるようにする**ためです——record はコンポーネントを1つも足せません。ドキュメントの例はもともと `Principal.of(...)` なので、そのとおりに書いていれば影響はありません |
| **`Dialect` を自分で実装していませんか**（D-157） | `Dialect` が `sealed` になり、**実装は MySQL と PostgreSQL の2つだけ**になりました。もっとも、`Dialects.of()` は最初からべた書きの表で製品を選んでおり、**自前の方言を設定から選ぶ道はありませんでした**——**開いている顔をして閉じていた**のを、見たとおりにしただけです。対応してほしい製品があれば、枠組みに足す形で受け付けます |
| **同じ名前の `AttributeKey` を2つ作っていませんか**（D-157） | 同名のキーが2つあると**起動時に落ちます**。動き自体は正しくなった（下記）のですが、**ログで見分けが付かない**ためです。どちらかの名前を変えてください |

### 変わったこと（挙動）

| | |
|---|---|
| **`cookie.secret` をあとから入れられるようになりました**（D-159） | `cookie.accept_unsigned = true` の間は、**署名の無い Cookie も読みます**。以前は `cookie.secret` を初めて設定した瞬間に `sid` / `csrf_token` / `remember` / flash が**全部いっぺんに捨てられ**、**全員ログアウト、フォームは 403** でした——しかも**例外もログも出ません**（署名の合わない Cookie を捨てるのは正しい動きなので）。書くほうは最初から署名するので、放っておけば入れ替わります。**入れ替わったら false に戻してください**（通した回数は `cookie.unsigned` に出ます）。手順は [セッションと Cookie の安全](https://jimble.io/ja/session-security) に |
| **属性キーは「作ったインスタンスそのもの」になりました**（D-157） | `AttributeKey` は record だったので、`equals` が**名前と既定値の両方**を見ていました。つまり、アプリが偶然 `new AttributeKey<>("auth_public", false)` と書くと **`Auth.PUBLIC` の枠を奪い、書いた覚えのないルートが公開になりました**。逆に、名前が同じで既定値だけ違えば**別のキー**になり、**入れたのに既定値が返りました**。**どちらもコンパイルは通り、例外も出ず、属性だけが効きません**——しかも前者は**開く側に倒れます**。いまはインスタンスで見分けるので、どちらも起きません。書き方は [ルーティング](https://jimble.io/ja/routing) に |
| **遅延読み込みは、書き換える口でも読み込みを起こします**（D-157） | `AsyncData` / `AsyncList` は**読む側だけを1つずつ上書き**していました。だから `remove` / `putIfAbsent` / `computeIfAbsent` / `add` / `set` / `clear` は、**未読み込みの空のマップやリストをそのまま触っていました**。`remove("id")` は空のマップから消すので、**あとで誰かが読んだ瞬間に消したはずの値が戻ってきます**。`add` した1件は、読み込みで**上書きされて消えます**。落ちも警告も出ません。いまは `Data` に**触る前の口を1本**置いて、`LinkedHashMap` / `ArrayList` の公開メソッドを全部そこへ通しています——**上書きし忘れたメソッドという概念が無くなりました**（`toString()` / `equals()` / `hashCode()` だけは通しません。ログやデバッガが覗いただけで SQL が飛ぶのは困るためです）。書き方は [遅延読み込み](https://jimble.io/ja/async) に |
| **失敗したあと巻き戻すかどうかは、呼んだ側が決めます**（D-156） | 各文の `catch` が**その場で `rollback()` を呼んでいた**のをやめました。**トランザクションの外では1行も仕事をしていませんでした**が、中では別の意味がありました——PostgreSQL は1文でも失敗すると **ROLLBACK するまで以降の文を全部断る**ので、**「エラーが出ても続けて、残りをコミットする」という動きは、枠組みが黙って巻き戻していたから成立していた**のです。それが部分コミットの正体でした。いまは**巻き戻すかどうかを呼んだ側が決めます**——`SQL → commit → SQL（失敗）→ rollback → SQL → commit` と分岐して続けられます（`rollback()` がエラーの持ち越しも畳みます）。**失敗のあとの文がそのまま通るかどうかは、製品によって違います**——PostgreSQL は ROLLBACK まで全部断り、MySQL はそのまま通します。**どちらにも寄りかからないでください**。jimble が約束するのは「**コミットは拒まれ、1行も残らない**」ところまでです。書き方は [トランザクション](https://jimble.io/ja/transaction) に |
| **`DBTransaction.acceptError()` を消しました**（D-156） | 未リリースの追加でしたが、**壊れていました**——「A を入れる → 失敗 → `acceptError()` → B を入れる」で**残るのは B だけ**でした（失敗の時点で A が巻き戻っていたためです）。しかも PostgreSQL では、巻き戻さずに続けること自体ができません。**製品によって成否が変わる口は置かない**ので消しました。エラーを見て続けたいときは、上の `rollback()` の形で書いてください |
| **トランザクションの中でエラーが出たら、コミットしなくなりました**（D-155 / D-156） | jimble の DB は**エラーを例外ではなく戻り値で返します**。中の `db.update(...)` が `-1` を返しても処理は正常に終わったように見えるので、**そのままコミットしていました**。しかも失敗した文の中で枠組みが `rollback()` を呼ぶので、**そこまでの文は巻き戻り、そこから先の文だけがコミットされる**——例外もログも出ないまま、**データが半分だけ入る**壊れ方でした。いまは **`DB.commit()`** がロールバックして `CodeException`（`DB_004`）を投げます（`DBTransaction` を通さず `db.beginTransaction()` から直に書いた道も同じです）。**判定はトランザクション全体を見ています**（`db.isError()` は直前の1文しか見ないので、失敗のあとに成功する文が1つあるだけでは隠れません）。エラーを見て分岐して続けたいときは、**`rollback()` を呼んでから**書き直してください（上の行）。書き方は [トランザクション](https://jimble.io/ja/transaction) に |
| **`-Djimble.env=prod` で `isProduction()` が true になります**（D-155） | コードは **`"production"` としか一致していませんでした**。[デプロイ](https://jimble.io/ja/deploy)は最初から `-Djimble.env=prod` と書いていたので、**そのとおりに動かしていたアプリでは本番の分岐が丸ごと素通りしていました**。`prod` / `prd`、`stg` / `stage`、`dev` / `development` を同じものとして扱います（大文字小文字も問いません）。**環境別ファイルの名前は書いたとおりのまま**です（`application.prod.conf` を読みます）。**表に無い名前は、どれにも当たりません**——`producton` のような打ち間違いを黙って `local` に倒すと、**本番機が「ローカルです」と名乗ってしまう**ためです。起動時に1度だけ警告が出ます |
| **`Log.error(例外, …)` がスタックトレースを出すようになりました**（D-155） | 出力先（`Log.Sink`）に**例外を渡している呼び出しが1つもありませんでした**。例外は構造化データの1項目としては残っていたので、**ログは出ているのに「どこで落ちたか」だけが消えている**という形でした。**独自の `Sink` を差し替えている場合、これまで `null` だった第5引数に例外が来ます** |
| **OIDC でも二要素認証が掛かります**（D-155） | `Oidc.callback` が中で `Auth.login` まで進んでいたので、**「Google でログイン」を選ぶだけで二要素が飛んでいました**（0.6.0 で OIDC と二要素を同じ版に入れたのに、噛み合っていませんでした）。**`Oidc.callback(プロバイダ, 関数, "/login/code")` の第3引数**にコードを入れる画面を渡してください。渡すと、パスワードで入るときと同じように `Mfa.pending` へ倒します。**渡していない2引数の形は、二要素を有効にしている人が来たら 401 で断ります**——素通りさせるくらいなら入れないほうがよい、という判断です |
| **`BatchContext.isCancelOrdered()` が実際の中断指示を返します**（D-155） | **常に false でした。**中断の判定は `AbstractBatch` の側にあり、コンテキストには何も繋がっていませんでした——`BatchContext` の Javadoc は「ループの中でこれを定期的に確認し、安全に止める」と書いているのに、**そのとおりに書いたバッチは中断ボタンで止まりませんでした**。`AbstractBatch` を継承していないところ（Domain 層など）から `Context.current(BatchContext.class).isCancelOrdered()` で見られます。`orderCancel()` も指示元に伝わるので、**履歴が `canceled` で残ります** |

### 直した

| | |
|---|---|
| **`Dson.decodes(Configration, InputStream, …)` が呼ぶと落ちていました**（D-155） | `new Dson().decodes(conf, …)` と、**自分自身を呼んでいました**。呼べば `StackOverflowError` です。隣の3引数版は正しく `decode` を呼んでいたので、**4引数版だけが誰にも呼ばれないまま壊れていました**。落ちる場所が「JSON を読むところ」なので、渡した JSON が悪いのだと思って探すことになります |

### 足した

| | |
|---|---|
| **設定キーを全部ドキュメントに載せました**（D-159） | [設定キー一覧](https://jimble.io/ja/config#設定キー一覧)に **131 本**あります。**サイトに1行も出てこないキーが 30 本**ありました（`redis.settings.*` は 10 本まるごと）。**書いていないキーも公開 API** で、誰かが見つけて使えば 1.0 で凍ります。`ConfigKeysTest` が担保するので、**載せるか消すかを決めないまま増やせません** |
| **1.0 の前にしか直せない型を4つ確定させました**（D-157） | `Principal` / `Data`（と `AsyncData` / `AsyncList`）/ `AttributeKey` / `Dialect` です。**1.0 を出すと形を変えられなくなる**もので、非推奨期間を置いても移す先がありません。棚卸しの「`Dialect` は 1.0 後は両方できなくなる」は**半分違っていました**——**`sealed` にできるのは 1.0 前だけ**ですが、**`default` を付けて開けるのは 1.0 後でもできます**。閉じておけばあとで開き直せるので、閉じるほうを先にしました |
| **1.0 に向けた棚卸しをまとめました**（`docs/design-1.0.md`） | 公開 API **103 パッケージ（署名 4,514 行）と設定キー 126 本**を、(1) 型とシグネチャ・拡張点 (2) 既定値と振る舞い (3) 設定キーの名前と構造 の3つの観点で通しました。**60 件**を、1.0 を出したら直せなくなる順に並べてあります。この版で直したのは、そのうち**互換とは無関係な6件**です |

---

## 0.6.0（2026-09-12）

**「誰がログインしたか」を外にも中にも広げた版。**
[OpenID Connect](https://jimble.io/ja/auth) で「Google でログイン」ができるようになり、
[二要素認証（TOTP）](https://jimble.io/ja/auth)が入った。
あわせて、**ドキュメントを AI が読める形でも配るようになった**
（[`/llms.txt`](https://jimble.io/llms.txt) と skill 4本）。

**依存は1つも増えていない。**ID トークンの検証は JDK だけでやっている
（JWKS の `n`/`e`・`x`/`y` は `KeyFactory` で公開鍵に戻せる）ので、
`jimble-auth-oidc` のような新しいモジュールも出していない。**公開物は 0.5.0 と同じ 14 本**である。

**minor を上げた**のは、機能が増えるだけで既存の挙動を変えないため
（0.x のあいだの約束は[版と互換性](https://jimble.io/ja/versioning)）。

### 上げる前に見るところ

| | |
|---|---|
| **`auth_mfa` / `auth_mfa_recovery` という名前のテーブルを自分で持っていませんか** | 枠組みのテーブルが2つ増えたので、**この名前のテーブルはコード生成の対象から外れます**（`FrameworkTables.ALL` に入りました）。同じ名前を使っているなら、**次の `codegen` で生成済みのクラスが消えます**。名前を変えるか、`codegen.excludes` の扱いを見直してください |
| **二要素認証を使うなら、`cipher.key` を流用しないでください** | 秘密鍵の暗号化には **`auth.mfa.secret_key`** を使います。`hash.password.encrypt` の既定が **「`cipher.key` があれば true」**なので、**いま平文の BCrypt を保存しているアプリが `cipher.key` を足すと、保存済みのパスワードが「暗号化済み」として読まれて全員ログインできなくなります**。返るのは「IDかパスワードが違います」だけなので、原因に辿り着けません |

**既存のアプリに影響する変更は、この2つだけです。**
OIDC も二要素認証も新しく足したもので、使わなければ何も起きません
（テーブルも、`Mfa` を使ったときに初めて作られます）。

### 足した

| | |
|---|---|
| **「Google でログイン」ができるようになりました（OpenID Connect）**（F-W-31 / D-152） | `get("/auth/google", Oidc.start("google")).attribute(Auth.PUBLIC, true)` と、同じ形のコールバックを1行ずつ書くだけです。**認可コード + PKCE のみ**で、暗黙フローはありません。`state`・`nonce`・PKCE の検証子は枠組みがセッションに置いて突き合わせます。**ID トークンの検証に外部ライブラリを入れていません**——JWKS の `n`/`e`・`x`/`y` は JDK の `KeyFactory` で公開鍵に戻せ、検証は `java.security.Signature` がやります（依存は1つも増えていません）。`alg` は**ヘッダを信じず**表で引き、`none` と `HS*` は通しません。`iss` は**完全一致**、`aud` は複数なら `azp` まで、`exp`/`iat`/`nonce` も見ます。**断る理由は返しません**（401 だけ。どこまで通ったかを測らせないためです）。**メールが同じでも既存の利用者に自動では結び付けません**——引くのは `provider + sub` の組だけです。自動にすると、**メールを検証していないプロバイダが1つ混ざるだけでアカウント乗っ取り**になります。**アクセストークンは保管しません**（ここは「誰がログインしたか」までです）。設定は `auth.oidc.<名前>.*`、書き方は [ログインと認可](https://jimble.io/ja/auth) に |
| **二要素認証（TOTP）が入りました**（F-W-32 / D-153） | パスワードが合ったあと `Mfa.isActive(利用者 ID)` なら `Mfa.pending(context, principal)` でコードの画面へ送り、`Mfa.complete(context, code)` が通ったときだけログインします。**コードを入れるまでは「ログインしていない」扱いです**——`Auth.principal` は `Principal.ANONYMOUS` を返すので、**ルートを足した人が何もしなければ入れません**。登録は `Mfa.enroll(利用者 ID, 表示名)` が `otpauth://` の URI と回復コードを返し、**`Mfa.activate` でコードが合って初めて有効になります**（ここを1段にすると、**QR の読み取りに失敗した人が二度と入れなくなります**）。**秘密鍵は `auth.mfa.secret_key` で暗号化して持ちます（AES-256-GCM）。鍵が設定されていなければ `enroll` は断ります**——TOTP の秘密鍵はパスワードのハッシュと違い、**漏れたらその場でコードが作れます**（破る手間がゼロです）。**回復コードを 10 個出します**（DB には SHA-256 だけ。1つは1回だけ。残りは `Mfa.remainingRecoveryCodes`）。**一度通ったコードは、その窓が終わるまで通りません**（覗き見たコードの入れ直しを止めます）。総当たりは既存の `Lockout` で抑えます——**6桁は 100 万通りしかないので、抑えないと1日で当たります**。`Mfa.disable` は **`attribute(Auth.FULL_AUTH, true)` を付けたルートから**呼んでください（緩いと、**Cookie を盗んだ側が2要素を外せます**）。QR 画像は作りません（URI を渡すので画面側で描いてください。依存を増やさないためです）。SMS とメールはありません（SMS は **SIM の乗っ取りで抜かれます**）。設定は `auth.mfa.*`、書き方は [ログインと認可](https://jimble.io/ja/auth) に |
| **`auth.mfa.secret_key` を足しました**（D-154） | 二要素認証の秘密鍵を暗号化する鍵です。**`cipher.key` から分けました**——`hash.password.encrypt` の既定が **「`cipher.key` があれば true」**なので、**いま平文の BCrypt を保存しているアプリが二要素認証のために `cipher.key` を設定すると、保存済みのパスワードが「暗号化済み」として読まれて全員ログインできなくなります。**返るのは「IDかパスワードが違います」だけなので、設定を足したことと結び付きません（サンプルの結合テストで実際に16本落ちました）。暗号化も **`CipherUtil`（固定 IV・改ざん検知なし）から `Aead`（AES-256-GCM）に替えました**——`CipherUtil` は自分の説明に「新しく暗号化するものに使わないこと」と書いてあるものです。**0.5.0 には二要素認証が入っていないので、上げる人に影響はありません** |
| **認証のサンプルが二要素認証まで通るようになりました**（`examples/approval-auth`。D-154） | ログイン → コード → 登録 → 解除まで、**動かして確かめられる形**で1本に入っています。`Auth` / `Principal` / ブロック属性 / ロックアウト / remember-me / 二要素認証が**同じアプリの中で噛み合っている**ところが読めます。**「覚える」は二要素の後**です——印はセッションに預けておき、**コードが通ってから** `Remember.issue` を呼びます。先に呼ぶと、**コードを知らない人の手元に「次から素通りできる Cookie」が残ります**。コードを入れる画面だけ jte で、登録は JSON です。**QR は描いていません**（`otpauth://` の URI を返すので、画面側で描いてください。依存も外部のサイトも増やさないためです） |
| **ドキュメントを AI が読める形でも配るようになりました**（NF-D-09 / D-151） | **全ページが同じパスの `.md` でも取れます**（`https://jimble.io/ja/db` → `https://jimble.io/ja/db.md`）。実コードから引いているコード片（`snippet=`）は**埋めた状態**で出るので、サイトで見えているものと同じです。目次は [`/llms.txt`](https://jimble.io/llms.txt)、言語ごとの目次が `/ja/llms.txt`、**全文1枚**が `/ja/llms-full.txt` です（日本語 38 ページで約 280KB）。目次から要るページだけ取りに来る道と、1回で全部持っていく道の**両方を残して**あります |
| **AI 向けの skill を4本同梱しました**（NF-D-09 / D-151） | **AI に jimble を書かせると、放っておけば Spring のつもりで書きます**——`@RestController` も `@Autowired` も `@Transactional` も、jimble には存在しないのに。**自信があるとき、モデルはドキュメントを引きません**ので、先に入っている必要があります。`skills/` に **`jimble`**（入口。原則・注釈もDIも無いこと・Spring の癖の対応表）、**`jimble-db`**（SQL ビルダー・`Data` のネスト・トランザクション・コード生成の決まり）、**`jimble-web`**（フィルタと属性の効く範囲・入力と出力・セッション/CSRF・エラー処理・認証）、**`jimble-batch`**（バッチの登録順序・中断・チャンク、および DB のテーブルをキューにする MQ）の4本、約 36KB です。置き場は **`.claude/skills/`**（jimble 自身のリポジトリでもそのまま効きます）。**`jimble new` が同じものを配ります**ので、新しいプロジェクトは最初から入った状態で始まります——ビルドが `.claude/skills/` を CLI のリソースへ写しているので**写しはありません**。**同じ課題を書かせて確かめました**——無しは `Jimble.create()` / `Filter` / 生 JDBC といった**実在しない API を丸ごと創作**し、1本だけだと形は合っても識別子を外し、3本そろえると**識別子まで合いました**。**指しているページが実在するかは `AiDocsTest` が見ています**（skill はドキュメントより速く腐るためです） |

### 直した

| | |
|---|---|
| **`extractTableData` の説明が逆でした**（[DB を使う](https://jimble.io/ja/db)） | 「1テーブルぶんだけ取り出せます」と書いていましたが、**`extractTableData` は平らにしません**——`{post: {...}}` を**ネストしたまま**返すので、そのまま JSON にすると**入れ子が1段残ります**。平らにするのは `getData(テーブル)` か `flattenTable(テーブル)` です。skill を作るときに AI がこの一文どおりに書いて、**出力に余計な階層が1つ入った**ので気づきました |
| **「キューに積むのはコミットしてから」と読めました**（[トランザクション](https://jimble.io/ja/transaction)） | 「MQ と混ぜるとき」の節が**コミット後に積む例**で始まっていて、「DB のキューへ積むのはトランザクションの中」は**最後の1行**にだけ書いてありました。**逆に読むと、ロールバックしても通知だけが残ります。**表にして、<b>DB のキューは中・外へ出すもの（メール / 外部 API / SSE）は外</b>と、どちら側かが先に分かる形に書き直しました。skill を書かせたときに **AI が2つの skill の食い違いとして指摘して**気づきました |
| **`json()` のあとに `send()` が要るように読めました**（[リクエストとレスポンス](https://jimble.io/ja/request-response)） | 「`json()` は組み立てるだけで、送るのは `send()` です」と書いていましたが、**ディスパッチャが実行の終わりに送る**ので `send()` は要りません（サンプルもどれも `json()` で終わっています）。`send()` を明示するのは本文なしで終わらせたいときだけです |

---

## 0.5.0（2026-09-11）

**ログインと認可が組み立て済みで入った版。**
`before(Auth::guard)` の1行とルート属性で書けるようになり、
[ロックアウト](https://jimble.io/ja/auth)と
[ログインしたままにする（remember-me）](https://jimble.io/ja/auth)まで含んでいる。
**注釈も DI も使っていない。**

### 上げる前に見るところ

| | |
|---|---|
| **`auth_attempt` / `auth_remember` という名前のテーブルを自分で持っていませんか** | 枠組みのテーブルが2つ増えたので、**この名前のテーブルはコード生成の対象から外れます**（`FrameworkTables.ALL` に入りました）。同じ名前を使っているなら、**次の `codegen` で生成済みのクラスが消えます**。名前を変えるか、`codegen.excludes` の扱いを見直してください |
| **認証を足すなら `before` の並び順に気をつけてください** | `before(Remember.restore(...))` → `before(Auth::guard)` の順です。**`Auth::guard` はいちばん最初に近いところ**に置いてください——セッションを使うかどうかをここで決めるので、先に誰かが `session()` を触ると間に合いません |

**既存のアプリに影響する変更は、この2つだけです。**
認証まわりはすべて新しく足したもので、使わなければ何も起きません
（テーブルも、`Auth` を使ったときに初めて作られます）。

### 足した

| | |
|---|---|
| **ログインしたままにできるようになりました（remember-me）**（F-W-30 / D-150） | `before(Remember.restore(App::findPrincipal))` を `before(Auth::guard)` の**前に**1行足し、ログインのときに**印が付いていたときだけ** `Remember.issue(context, principal)` を呼びます。**盗まれる前提で作ってあります。**Cookie は `selector:validator` の2つ組で、**validator は使うたびに作り直します**——盗まれた Cookie と本物は同時に生きられないので、**回転前の値が使われたら盗まれた合図**です。合図が出たら**その利用者の記憶を全部消します**（どちらが盗んだ側かは区別できないので、片方だけ消すと**本人だけが締め出されて盗んだ側が残る**ことがあります）。**並列のリクエストでは締め出しません**——回した直後の 60 秒は古いほうも通します。期限は**最後に使ってから 30 日、かつ発行から 90 日**（使い続けても必ず一度は入り直します）。`validator` は **SHA-256 にして保存**します（そのまま持つと、DB を読めた人が全員になりすませます）。**ログアウトで消えます**（`Auth.logout` が消します）。**パスワードを変えたら `Remember.forgetAll(userId)` を呼んでください**——呼ばないと**盗まれた Cookie はそのまま使えます**。「全端末からログアウト」も同じものです。設定は `auth.remember.*`。書き方は [ログインと認可](https://jimble.io/ja/auth) に |
| **ルート属性 `Auth.FULL_AUTH` が増えました**（F-W-30 / D-150） | **「いまパスワードを入れた人」だけを通します。**remember-me で戻ってきただけの人は **401** になり、入り直すことになります。パスワードの変更・退会・決済・連絡先の変更に付けてください——**Cookie を盗まれたときの被害がここで止まるので、remember-me を出せます。**コードの中で見るなら `Auth.fullyAuthenticated(context)` ですが、**ルート属性のほうが書き忘れが起きません** |
| **ログインの失敗を数えて、次の試行を待たせるようになりました**（F-W-29 / D-149） | `Auth.attemptLogin(context, loginId, password, hash)` に替えるだけです（「待たせる → 照合する → **成功なら数えたものを消す**」までやるので、消し忘れられません）。**「N 回で M 分ロック」にはしていません**——アカウント単位で止める仕組みは**そのまま嫌がらせの道具になる**（わざと間違えるだけで締め出せます）ので、**待ち時間を倍にしていきます**：3回目までは待たせず、4回目から 1 → 2 → 4 …… 上限 300 秒、24 時間で数え直します。残っていれば **429 と `Retry-After`** を返します（画面へ飛ばすかはアプリの `error()` が決めます）。**数える単位は「入力されたログイン ID」です**——見つかった利用者の DB 上の ID を渡すと、<b>居ない ID のときだけ数えられず、待たされるかどうかでどの ID が在るかが分かってしまいます</b>。記録は `auth_attempt` テーブルに **SHA-256 で**入ります（平文では溜まりません）。掃除は自動です（失敗を数えたついでに1時間に1度）。**DB が無ければ何もしません**（ログに1度だけ出ます）。設定は `auth.lockout.*`、手で解除するなら `Lockout.clear(loginId)`。**[流量制限](https://jimble.io/ja/ratelimit)の代わりにはなりません**——あちらは IP ごとなので、1つのアカウントに 1000 個の IP から1回ずつ来ると発火しません。**両方掛けてください** |
| **ログインと認可が組み立て済みになりました**（F-W-28 / D-148） | `before(Auth::guard)` の1行と、ルート属性3つ（`Auth.PUBLIC` / `Auth.ROLE` / `Auth.NO_SESSION`）だけで書けます。**注釈も DI も使いません。****既定は「要ログイン」**なので、ルートを足した人が何も書かなければ閉じています。役割が足りないときは **403**（401 ではありません——ログインし直しても変わらないことを状態コードで言います）。`Auth.login` は<b>セッション ID を振り直してから保存します</b>。`Auth.principal(context)` は<b>`null` を返しません</b>（`Principal.ANONYMOUS`）。**`Auth.checkPassword` を使ってください**——`PasswordUtil.check` はハッシュが `null` だと**即座に false を返す**ので、**利用者がいないほうが目に見えて速く**なり、応答時間で ID を数えられます。**401 のときに転送はしません**（画面へ飛ばすか JSON かは、アプリの `error()` が決めます）。<b>JWT は出しません</b>（失効できないため）。remember-me・OAuth はまだありません。`examples/approval-auth` は<b>319 行から 219 行になり、結合テストは1行も変えずに通りました</b>。書き方は [ログインと認可](https://jimble.io/ja/auth)（日英・新設）に |
| **ルート属性をブロック単位で書けるようになりました**（F-R-26 / D-147） | `attribute(PUBLIC, true)` をブロックに書くと、**そのブロックの中のルート全部に付きます**。ルート1本ずつに書かなくて済みます。強い順に <b>ルート > 内側のブロック > 外側のブロック > キーの既定値</b>で、書く場所は問いません（ブロックの最後に書いても、その前に登録したルートに付きます）。**フィルタと同じで、パスには付きません**——同じパスでも別のブロックで登録したルートには付きません（D-69）。<b>`rateLimit()` はこの仕組みの上に乗り換えました</b>（挙動は変わりません。特別扱いが1つ減っています） |
| **ログイン時にセッション ID を振り直せるようになりました**（F-S-13 / D-147） | `context.session().regenerateId()` です。**ログインの前後で ID が変わらないと、ログイン前に仕込まれた ID がそのまま権限を持ちます**（セッション固定化）。中身は持ち越すので、戻り先の URL や CSRF トークンは消えません。<b>保存はしません</b>（自動保存をしない決まりのままです）——振り直したあと `save()` を忘れると、**古い側は消えていて新しい側は無い**＝ログインしていない状態になります（**閉じるほうに倒れます**）。DB / Redis は古い行を消して新しい ID で書き直し、Cookie は中身を書き直すだけ、`none` は何も起きません |

---

## 0.4.0（2026-09-11）

**閉じ忘れた DB コネクションを実行の終わりに拾うようにした版。**
あわせて、[脆弱性の報告窓口](https://jimble.io/ja/security)と
[版と互換性の約束](https://jimble.io/ja/versioning)を決めた。

### 変わったこと（挙動）

| | |
|---|---|
| **コネクションを握ったまま実行が終わると、エラーログが出て閉じられます**（F-D-16 / D-143） | いままで拾っていたのは <b>`DBTransaction` を使って始めたトランザクションだけ</b>でした。これからは <b>`db.beginTransaction()` を直に呼んだとき</b>と、<b>カーソル（`selectListWithFetcher`）を開いたまま `DB` を閉じなかったとき</b>も拾います。**ふつうの SQL は元から漏れていません**——1文ごとにコネクションをプールへ返しているので、`DBUtil.getMainDB()` を使い捨てにする書き方はそのままでかまいません。**閉じ忘れていたコードでは、いままで出ていなかった ERROR が出るようになります**（`閉じられていない DB が残っていました。閉じます: <DB名>`）。<b>ログが増えたのではなく、いままで黙って漏れていたということです。</b>畳み忘れのメッセージにも DB 名が付きました |

### 足した

| | |
|---|---|
| **ドキュメントだけでアプリを作れるか、もう一度確かめました**（NF-D-01 / D-146） | 1回目（0.2.0）で35ページ直したあと、**サンプル7本とサイトが揃った状態で**やり直しました。**ビルドファイルは貼るだけで動き、Java も jte もテストも1回目のビルドで通りました**（1回目は<b>そもそも始められません</b>でした）。詰まったのは**設定まわりの4件だけ**で、直してあります：<b>PostgreSQL の設定例が `password = ${?DB_PASSWORD}` の1行だけ</b>だった（`${?ENV}` は環境変数が無いと**キーごと消えます**。そのまま真似すると PostgreSQL の SCRAM のエラーで止まり、設定の話に見えません）、<b>`create_database_sql` の説明がどこにも無かった</b>、<b>マイグレーションの例が MySQL の DDL だけ</b>だった（**マイグレーションは方言を吸収しません**）、<b>時刻をアプリ側とDB側のどちらの時計で入れるかが書かれていなかった</b>（`Dsl.now()` がサイトに0件でした）。記録は `docs/docs-only-trial.md` |
| **版と互換性の約束を決めました**（NF-C-03 / NF-C-04 / D-145） | jimble.io の[版と互換性](https://jimble.io/ja/versioning)（日英）です。**0.x のあいだはマイナーで壊れうる**と明記し（SemVer の定義どおり）、**非推奨期間の約束は 1.0 から**発効させます。ただし<b>0.x でも黙って壊すことはしません</b>——壊れうる変更は **CHANGELOG の「変わったこと（挙動）」に必ず出します**。**どこまでが公開 API か**をパッケージ単位で決め、[`docs/api-packages.txt`](https://github.com/hidemikimura/jimble/blob/main/docs/api-packages.txt) に全 154 パッケージを公開 101 / 内部 49 / 空 4 で並べました（<b>`public` だから公開 API ではありません</b>。Java には「見えるが使ってほしくない」を表す修飾子が無いので、内部実装も `public` です）。**この一覧は古くなりません**——パッケージを増やして書き忘れると `ApiSurfaceTest` が落ちます。<b>クラスは動かしていません</b>（`internal` へ移すと、いま使っている人の import が全部壊れるためです） |
| **脆弱性の報告窓口を作りました**（NF-S-08 / D-144） | `SECURITY.md` と、jimble.io の[セキュリティ](https://jimble.io/ja/security)（日英）です。受け口は **GitHub の非公開報告1本**で、メールアドレスは公開していません。**約束するのは受領の返事（数日以内）だけ**で、修正の期限は約束していません（個人で開発しているためです）。**修正は最新の版に入れます**——古い版へのバックポートはしません。<b>対象外もはっきり書きました</b>：`examples/` のサンプル（接続情報も鍵も既定値のままです。**本番に置かないでください**）、依存そのものの脆弱性（Dependency graph と Dependabot で追っています）、設定でそう決めた動き（`server.trust_proxy = true` など）。**ただし<b>ドキュメントと実装が食い違っているなら脆弱性として扱います</b>** |

### 直した

| | |
|---|---|
| **サンプルの CSV 出力が、呼ばれるたびにプールからコネクションを1本ずつ消していた**（F-D-16 / D-143） | `examples/approval-list` と `examples/blog` の CSV 出力です。`ResultSetFetcher` は畳んでいましたが、<b>`DB` を畳んでいませんでした</b>。カーソルは読み終わるまで `ResultSet` を開けておくので、<b>`close()` までコネクションを握ったまま</b>です。**SQL は成功し、例外も警告も出ない**ので、プールが枯れるまで誰も気づきません。`sql.md`（日英）の「大きい結果」の例も同じ形で、しかも載っていた `row -> {}` は <b>`selectListWithFetcher` が `ResultSetFetcher` を取るのでコンパイルも通りません</b>でした。動く形に直し、`transaction.md`（日英）に<b>「どこで握るとどこで返るか」の表</b>を足しました |

---

## 0.3.0（2026-09-11）

**サンプルを7本書いて、そこで見つかったものを直した版。**
機能ごとに単体で完結するサンプル（`examples/approval-*`）を7本足し、
<b>書いていて踏んだ不具合とドキュメントとの食い違いを、見つかった分だけ全部直した</b>。
あわせて依存を **21.5MB** 減らしている
（アプリ1本の実行時クラスパスが 39.5MB / 102 jar → 18.0MB / 86 jar）。

### 上げる前に見るところ

**挙動の変わる変更が多い版である。**下の「変わったこと（挙動）」が全部だが、
<b>黙って結果が変わりうる</b>のは次の5つなので、先に挙げておく。

| | |
|---|---|
| **パスが合っていてメソッドだけ違うと 405** | いままでは 404。<b>「404 が返ること」を確かめているテストは落ちる</b> |
| **`error` を書いていないときのエラー応答に本文が入る** | いままではステータスだけ。<b>空の本文を前提にしたテストは落ちる</b> |
| **`insertBatch` が `null` を返しうる** | 積んだビルダーの SQL が揃っていないとき。<b>いままでは例外も警告も無しに値が横にずれて入っていた</b>ので、直った側が「書けていない」に見える |
| **スケジューラを止めて抜けたバッチが `canceled` で残る** | いままでは `completed`（完了）だった |
| **`JIMBLE_TEST_*` はサンプルに効かない** | `examples/` は `JIMBLE_SAMPLE_*` を見る。**jimble 本体を使うアプリには関係ない** |

### 変わったこと（挙動）

| | |
|---|---|
| **パスが合っていてメソッドだけ違うと 405 になります**（F-R-25 / D-11） | いままでは 404 でした。`Allow` ヘッダも付きます（`Allow: GET`）。**「404 が返ること」を確かめているテストは落ちます。**404 は「そんなものは無い」、405 は「あるが、その呼び方ではない」で、混ぜると<b>`post` と書くべきところを `get` と書いただけ</b>の間違いが「パスが違う」に見えます。擬似メソッドの `WS` は `Allow` に出しません |
| **`error` を書いていないときのエラー応答に本文が入ります**（F-C-17 / D-11） | いままでは<b>ステータスだけで本文が空</b>（`Accept: application/json` のときだけ `{}`）でした。これからは JSON を名指しされたら `{"error":{"status":404,"message":"Not Found"}}`、そうでなければ `404 Not Found` の短いテキストです。**`error(...)` で本文を組み立てていれば、送信前でもそちらが勝ちます**（挙動は変わりません）。**中身は決め打ちで、例外のメッセージも SQL もスタックトレースも入りません**（NF-S-06）。空の本文を前提にしていたテストは落ちます |
| **`acceptJson()` が q 値つきの Accept を拾うようになりました**（D-11） | `application/json;q=0.9` も、並びの2つ目以降で前に空白があるもの（`text/html, application/json`）も、いままでは **false** でした（`,` で割った断片を丸ごと比べていたため）。**`q=0`（要らない）は false**、**`*/*` は今までどおり false** です（`*/*` を true にすると<b>ビューを返すルートが JSON を返す</b>ようになります）。JSON を求めていたのに画面が返っていた箇所があれば、そこの挙動が変わります |
| **一生呼ばれないルートを起動時に警告します**（F-R-13 / D-10） | `/users/{id}` と `/users/{userId}` を両方 GET で登録するような、<b>どんなリクエストでも他が先に当たる</b>ルートです。**既定は警告だけで、起動は止めません。**`server.strict_routes = true` にすると例外になります（CI ではこちらを推奨）。`/users/me` が `/users/{id}` より先に当たるような<b>正しい動きは警告しません</b> |
| **MQ のテーブルに列が1つ増えます**（NF-O-05 / D-126） | `traceparent varchar(64)`（null 可）。分散トレーシングで、**積んだところと処理したところを1本のトレースで繋ぐ**ために使います。`MqTables.install(...)` が起動時に当てるので手で流す必要はありませんが、**キューのテーブルに `ALTER` が1回走ります**。トレースを使わないアプリでは、この列はずっと null のままです。**アプリの内容（`data`）には触りません** |
| **`in()` / `not_in()` に空の一覧を渡すと例外になる**（F-D-07 / D-102） | いままでは `IN ()` という構文エラーの SQL を組み立てて DB に投げていた。**SQL を組み立てたところで `SqlBuildException`** にする。「空なら条件ごと外す」ことはしない（`in(空)` は「どれにも当たらない」、条件を外すと**全件**。取り違えると静かに全件消したり全件見せたりする）。空になりうるところは `if (ids.isEmpty())` で分けること |
| **ウィンドウ関数を `where` / `having` に書くと例外になる**（D-103） | `Dsl.rowNumber().over(...).eq(1)` は SQL の決まりで書けない。いままでは書けてしまい、**DB に投げるまで気づけなかった** |
| **バッチの登録が0件のまま `BatchRegistry.sync` を呼ぶと、`batch_master` の行が全部 `nothing` になる**（D-104） | いままでは0件のとき何もしなかったため、**最後の1つを消したときだけ行が `enable` のまま残って**いた。「1つも登録されていない = 全部消えた」に揃えた。**`sync` は登録を済ませてから呼ぶこと。**バッチを持たないアプリは呼ばないこと |
| **スケジューラを止めて抜けたバッチが、履歴に `canceled` として残ります**（F-B-06 / D-137） | いままでは **`completed`（完了）** として記録されていました。バッチは `isCancelOrder()` を見てループを抜けるのに、抜けたあとの判定が<b>親（スケジューラ）からの中断を見ていなかった</b>ためです。「途中で止めたのに完了になっている」履歴が出なくなります。**`doCancel()` と管理画面からの中断は、いままでどおり `canceled` です。**「スケジューラを止めたあとの履歴が `completed` であること」を確かめているテストがあれば落ちます |
| **サンプルの接続先を差し替える環境変数の名前が変わりました**（D-138） | `examples/` のサンプルは **`JIMBLE_SAMPLE_DB_*` / `JIMBLE_SAMPLE_PG_*`** を見ます（`JIMBLE_TEST_*` から変更）。**`JIMBLE_TEST_*` は jimble 本体のテスト（`jimble_test`）専用になりました。**いままでは同じ名前だったので、`dbTest` / `pgTest` の接続先を替えるつもりでこの環境変数を立てると<b>サンプルのマイグレーションが `jimble_test` へ流れ込み</b>、履歴が混ざって<b>jimble 本体のマイグレーションテストが落ちて</b>いました（しかも落ち方が「フレームワークの不具合」に見えます）。**サンプルを既定の接続先で動かしているなら影響はありません。**`JIMBLE_TEST_*` でサンプルの接続先を替えていた場合は、`JIMBLE_SAMPLE_*` に読み替えてください |
| **`insertBatch` に SQL の揃っていないビルダーを混ぜると `null` が返ります**（F-D-08 / D-141） | いままでは<b>先頭のビルダーの SQL だけを使い、残りのビルダーからはパラメータだけを取って</b>いました。`value()` を積む順が違うビルダーが混ざると、<b>例外も警告も無しに値が横にずれて入ります</b>（`amount` の列に `status` が入るなど）。これからは SQL を1本ずつ突き合わせ、違ったら `DB_998` を立てて `null` を返します。**戻り値を見ていないコードは、いままで壊れたデータを書いていた箇所で「書けていない」に変わります。**揃ったビルダーしか渡していないなら影響はありません。`executeBatch` は元から SQL を突き合わせています |
| **`FileUtil.FileMetaData` の `delimiter` と `charset` の中身が変わった**（D-116） | `delimiter` は tika の名前（`comma`）ではなく**区切り文字そのもの**（`,` / `\t` / `;` / `|`）を返します。`charset` は `Charset` の正式な名前（`UTF-8`。以前は小文字）です。画像・その他のときは `null` ではなく空文字を返します。`contentType` は**パラメータを落とした小文字**で返します |
| **HTTP クライアントが zstd を扱わなくなった**（D-121） | `Accept-Encoding` から `zstd` を外しました（gzip / deflate / br は変わりません）。展開に 2.9MB のライブラリが要り、**1行の分岐のためだけに抱えるには大きすぎた**ためです。名乗らなくなるので、まともなサーバは zstd で返してきません |
| **redisson の Rx / Reactive / LiveObject が使えなくなった**（D-121） | `redissonClient.reactive()` / `rxJava()` / `getLiveObjectService()` を呼ぶと `NoClassDefFoundError` になります。jimble はどれも使っておらず、**この3つのためだけに 13.5MB**（byte-buddy / rxjava / reactor-core / jodd-util）が付いてきていました。必要な場合は、それぞれのライブラリを自分の依存に足してください |
| **`IcuUtil.convertToHiragana(...)` が「ー」を残すようになった**（D-120） | いままでは `コーヒー` → **`こおひい`**、`サーバー` → `さあばあ` と、直前のかなの母音に開いていました（ICU の決まり）。これからは `こーひー` `さーばー` と<b>そのまま残ります</b>。`convertToKatakana` で往復すると元に戻ります。`ヶ` → `け`、`ヵ` → `か` は変わりません。あわせて、かなの変換のついでに起きていた**無関係な文字の Unicode 正規化**（チベット文字の合成、結合文字の並べ替え）が無くなりました |
| **`FileCharDetecter.detector(...)` の返す名前が変わった**（D-119） | `Charset.name()` の正式な名前を返します（`SHIFT_JIS` → `Shift_JIS`、`WINDOWS-1252` → `windows-1252`）。`Charset.forName(...)` に通す使い方なら影響しません。あわせて**言い切れないときは既定の文字コードを返す**ようになりました（純 ASCII や空のファイルに当てずっぽうを返しません） |
| **`Dialect` に `sqlSyntax()` が増えた**（D-114） | SQL の字面の決まり（文字列・識別子・コメントの見分け方）を返す。`jimble-db` の `MySqlDialect` / `PostgreSqlDialect` は対応済み。**自分で `Dialect` を実装している場合は追加が要る**（`SqlSyntax.MYSQL` / `SqlSyntax.POSTGRESQL` をそのまま返せます）。既定値を持たせなかったのは、黙って MySQL の読み方になるほうが危ないためです |
| **マイグレーションで1文も取り出せなかったら失敗にする**（D-114） | up や down が全部コメントだったとき、いままでは成功として通していた。down でこれが起きると<b>テーブルは残ったまま履歴だけ消える</b> |
| **適用済みのマイグレーション SQL の名前を変えると、起動時に止まる**（F-G-20 / D-113） | 適用済みはファイル名で覚えているので、`001_create_post.sql` を `001_create_post.mysql.sql` に変えると**別のファイルとして同じ SQL がもう一度流れる**（`Table 'post' already exists`）。**製品の接尾辞を落とした名前**でそれを見分けて止め、**履歴を付け替える `UPDATE` 文を出す**（中身も変わっていれば `hash` も）。`examples/blog` を使っている環境では、その `UPDATE` を流すか `blog_example` を作り直すこと |
| **`codegen` が `sql_cache` / `sql_cache_tag` / `rate_limit` を生成しなくなった**（D-68） | jimble が作るテーブルなのに除外一覧から漏れていて、**アプリのテーブル定義クラスとして生成されていた**（`rate_limit` は `io.jimble.web.ratelimit.RateLimit` と単純名がぶつかる）。これらのテーブルがある環境では `codegen` の出力が変わるので、**生成物をコミットしているなら流し直すこと** |

### 直した

| | |
|---|---|
| **`subs`（ぶら下げたサブ DB）の説明が逆だった**（F-D-14 / D-141） | `db.md`（日英）に「<b>こちらは接続を共有します</b>」「<b>トランザクションも共有されます</b>」と書いてありましたが、<b>どちらも逆</b>です。`subs` は<b>親とは別のプール</b>で、同じデータベースを指していても別の接続なので、親のトランザクションの中で `newSubDB` に書いても<b>一緒には戻りません</b>。あわせて、<b>`subs` にはマイグレーションも codegen も当たらない</b>こと（どこにも書かれていませんでした）、<b>`subs` が別のデータベースを指すならそれが先に在る必要がある</b>こと、<b>引数なしの `DBUtil.getDB()` は存在しない</b>こと（`getMainDB()` です）を直しました。`sql.md`（日英）の「<b>戻り値は件数のリスト</b>」も `insertBatch` には当てはまりません（採番値の `List<Long>` です）ので分けて書き、SQL を揃える必要も足しました。**コードは変えていません** |
| **テンプレートのドキュメントにレイアウト継承の記述が無かった**（F-W-08 / F-W-10 / D-140） | `view.md`（日英）に<b>枠の共有・部品・コメント</b>の節を足しました。レイアウトを共有する書き方は、これまで<b>ドキュメントにもサンプルにも1件もありませんでした</b>。あわせて<b>jte のコメントは `<%--` `--%>` である</b>ことを明示しました——`@*` `*@` は<b>コメントにならずそのまま出力され、しかも中に書いた `@template.` が実行されます</b>（レイアウトの中に自身の呼び出し例を書くと `StackOverflowError` になります。実際に踏みました）。`assets.md`（日英）も直しました：<b>そのまま真似できない例</b>（`test-assets` は `src/test/resources` にあります）、<b>「SPA / MPA も同じ」と読める記述</b>（登録されるルートの本数が違います。Asset は2本、SPA / MPA は4本）、<b>`AssetHandler` は `/assets` 自身には応えない</b>こと。**コードは変えていません** |
| **`isScheduler()` の説明が足りず、取り違えやすかった**（F-B-04 / D-139） | `AbstractBatch.isScheduler()` は「**このバッチがスケジューラそのものか**」で、<b>「スケジューラに載せるか」ではありません</b>。cron のつもりで `true` にすると `BatchRegistry.sync()` が飛ばすので<b>マスタに行ができず、スケジューラから見えないまま</b>になります。手で流せば動いて履歴も残るので、<b>気づきにくい壊れ方</b>です（サンプルを書いていて実際に踏みました）。javadoc に書き足し、cron で回すのに要るのは `cron()` を書くことだけだと明示しました。**コードの挙動は変えていません** |
| **集計を条件にすると、左辺の消えた SQL が組み上がっていた**（F-D-09 / D-135） | `HAVING SUM(...) >= ?` を組む道が無く、無理に組むと <b>`HAVING ( >= ?)` という壊れた SQL が例外も警告も無しに出来上がって</b>いました。つまり<b>集計に対する `having` は一度も書けたことがありません</b>でした。これからは `Dsl.sum(Request.amount).ge(500_000L)` と、<b>列のときと同じ形</b>で書けます（`CASE` の条件にも置けます）。**足したのは比較の9つだけ**です（`eq` / `not` / `gt` / `lt` / `ge` / `le` / `between` / `is_null` / `is_not_null`）。**既存のメソッドのシグネチャは1つも変えていません**（公開済みの jar との互換性はそのままです）。組み上がる SQL の字面も変わりません |
| **空のまま送られた入力欄が、必須の検証を素通りしていた**（F-V-07 / D-133） | `<input name="title">` を<b>空のまま送ると</b>、値が「空文字」ではなく<b>「空のリスト」</b>として届いていました。`required()` / `empty()` が見るのは「null か、空の文字列か」なので、<b>どちらにも当たらず通ってしまいます</b>。これからは空文字として届きます。**「空で送った」と「そもそも送っていない」の区別は残ります**（送っていない項目はキーごと届かないので、`PATCH` で「送った項目だけ変える」書き方は変わりません） |
| **検証に落ちたときに、入力値が返ってきていなかった**（F-W-09 / D-133） | `ValidationExecutor` は落ちたときに `putForm` で入力値を保持していましたが、<b>その値は応答にもテンプレートにも出てきませんでした</b>（誰も読まないフィールドに入っていました）。ドキュメントには最初から「入力値も一緒に返る」と書いてあったので、実装をそちらに合わせました。<b>422 の本文に、送られてきた項目がそのまま載ります</b>。`putForm` / `setForm` を自分で呼んでいる場合も同じで、入れた値が応答に載るようになります |
| **Cookie と Flash に日本語を入れると化けていた**（D-132） | `flash().put("message", "登録しました")` のような値が、<b>例外も警告も出ないまま `????` になってブラウザへ届いて</b>いました（HTTP のヘッダは ASCII なのに、値をそのまま書いていたためです）。これからは percent-encode して送り、受け取るときに戻します。**`sid` と `csrf_token` は元から ASCII なので影響ありません。****署名つき Cookie の見た目も変わりません**（`|` も `=` も `+` もそのまま通します）。<b>1つだけ直しようのないものがあります</b>：すでにブラウザにあって、<b>値に `%` が入っている Cookie</b>は、版を上げたあと一度だけ読み違えます（`%41` が `A` になる。次に書き直された時点で直ります）。アプリが `%` を含む値を Cookie に入れている場合はご注意ください |
| **DB が読み込めていないと、DB の話をしない例外で落ちていた**（F-X-05 / D-130） | `DBUtil.load` は<b>繋がらなくても例外を投げず、原因をログに出して `false` を返します</b>。その戻り値を見ずに先へ進むと、サーバーは起動してしまい、最初のリクエストで `NullPointerException: Cannot read field "conf" because "dbSource" is null` になっていました（<b>本当の原因は何十行も上のログにあります</b>）。これからは `DBUtil.getMainDB()` / `getDB(名前)` が<b>取り出したその場で</b>、どの DB の話か・何を直せばいいかを書いた `IllegalStateException` で落ちます。`DBUtil.load` そのものの戻り値は変えていません（`db` の設定が無ければ `true`）。**呼ぶ側で戻り値を見てください**——`db.md` / `pitfalls.md` と `examples/blog` を直しました |
| **`cookie.secret` を設定すると Cookie セッションが丸ごと効かなくなっていた**（F-S-08 / D-128） | セッションの Cookie を<b>署名せずに</b>書いていたのに、受け取り側は<b>すべての Cookie の署名を検証して、落ちたものを捨てる</b>。つまり署名鍵を設定した瞬間に `session.store = "cookie"` が動かなくなっていました。<b>例外もログも出ず、「保存したのに次のリクエストで消えている」だけ</b>が残る形です。署名も通すようにしました（大きさの上限も署名を含めて見ます） |
| **Cookie セッションの有効期限を、中身を見ずに延ばしていた**（D-128） | 読めなくなった Cookie も、踏まれるたびに寿命が更新されて<b>永久に残り続けて</b>いました。読めるものだけ延ばします |
| **ドキュメントが署名検証をすり抜けるコードを教えていた**（D-128） | `session-security.md` が「読むときは `unsignCookie("名前")`。改ざんされていたら `null`」と書いていましたが、<b>`unsignCookie()` は署名を検証しません</b>（返るのは受信した生の値で、無いときは `null` ではなく空文字）。正しくは `cookie("名前")` です。日英とも直しました。**コードは変えていません** |
| **`jimbleRun` を止めて動かし直すと、次のビルドが起動しない**（D-101） | helidon が起動のときに JVM 全体の直列化フィルタを張る。`jimbleRun` はアプリを Gradle デーモンの中で動かす（D-77）ので、**フィルタがデーモンに残り**、次のビルドが `Couldn't populate class org.gradle.api.services.BuildServiceParameters$None > filter status: REJECTED` で落ちていた。`jimbleRun` のあいだだけ `helidon.serialFilter.missing.action = IGNORE` にする。**本番の挙動は変わらない** |
| **マイグレーション SQL の切り分けが、製品にかかわらず MySQL の読み方だった**（F-G-05 / D-114） | バックスラッシュを常にエスケープとして読むので、PostgreSQL の `insert into t values ('c:\');` で**文字列が閉じず、後ろの SQL が全部1文にくっついて**いた。文字列・識別子・コメントの見分け方を方言に聞くようにした（`Dialect.sqlSyntax()`）。`#` の行コメント（MySQL だけ）、`--` のあとの空白（MySQL は要る）、ドル引用符 `$tag$ ... $tag$`（PostgreSQL の関数の本体・`DO` ブロック）、入れ子のブロックコメント（PostgreSQL）も読み分ける |
| **行コメントを書くと、そこから先の SQL が全部コメントになっていた**（D-114） | 読んだ直後に改行を空白へ潰していたので、`--` や `#` が**行末で終わらなかった**。潰すのは**ハッシュを取るときだけ**にした（潰し方は変えていないので、適用済みのハッシュは変わりません）。コメントだけになった断片は捨てます（MySQL の `/*! ... */` は実行されるコメントなので捨てません） |
| **SPA を `/` に置くとトップページだけ 404** | `"/*"` はセグメントが0個の `/` に当たらないのに、`/` 自身を登録していなかった。`/any` は 200 で返るので気づきにくかった |
| **`in` の空一覧が JSON の `where` からだと素通りしていた** | `{"where": {"site": {"id|in": []}}}` が `IN (NULL)` になり、**例外もエラーも出ずに 0 件**（`not_in` なら本来の全件が 0 件）。`in(null)` も同じく落とすようにした |
| **エラーハンドラが送信してから落ちると、後続のエラーハンドラが走っていた** | 「送信済みなら以降は実行しない」の判定を `catch` で飛ばしていた |
| **ドキュメントだけでアプリを1本作れなかった**（`docs/docs-only-trial.md`） | ドキュメントと Maven Central の 0.2.0 だけでアプリを作る実地確認をしたところ、**そもそも始められませんでした**（依存の座標も動くビルドファイルもサイトに無い）。日英 35 ページずつに次を入れました：**そのまま動く `settings.gradle.kts` / `build.gradle.kts` の全文**とモジュールの表、リポジトリ URL、**「サイトは開発中の版に追従している」の明示と 0.2.0 に無い節の印**、`getString` / `getStringOptional` の説明の訂正（逆でした）、**`context.request()` から直接は読めない**、`DBUtil.load(...)` を DB のページに、クラスの置き場所の表、`gradle wrapper --gradle-version`。**フレームワークのコードは変えていません** |
| **依存の一覧が GitHub に一度も登録できていなかった**（NF-S-07 / D-118） | `dependency-submission` が9回とも失敗していた。原因はワークフローではなく<b>リポジトリの設定で Dependency graph が無効</b>だったことで、`The Dependency graph is disabled for this repository` は**ログ本文ではなく Annotations にしか出ない**ため、最後まで読んでも成功したようにしか見えなかった。設定を有効にして通した。ワークフローの先頭にこの前提を書いた |
| **GitHub Actions の action を上げた**（D-118） | Node 20 の非推奨警告を消すため、`actions/checkout` を v7、`actions/upload-artifact` を v7、`actions/setup-java` を v5 に。**`gradle/actions` は v5 で止めた**（v6 はキャッシュが MIT ではない非公開コンポーネントになり、使うと Gradle の Terms of Use への同意になる）。Dependabot が毎週 v6 を出してこないよう `ignore` を入れ、上限を 3 から 5 に上げた |
| **commonmark を 0.26.0 → 0.30.0 に上げた**（D-123） | 壊れる変更はありません。病的な入力に対する<b>スタックオーバーフローと二次関数的な遅さの修正</b>が入り、表のセル数（100 万）と入れ子の深さ（100）に既定の上限が付きました。**ドキュメントサイトの全 70 ページを新旧で描き比べて、生成される HTML が 1 byte も違わない**ことを確認しています |
| **依存を棚卸しして 21.5MB 減らした**（NF-L-01 / D-121） | アプリ1本の実行時クラスパスが **39.5MB / 102 jar → 18.0MB / 86 jar** になりました。`guava`・`commons-validator`（＋古い4本）・`commons-text`・`aircompressor` を外し、redisson の使っていない枝（byte-buddy / rxjava / reactor-core / jodd-util）を切りました。**置き換えたものはすべて、置き換える前と突き合わせて答えが変わらないことを確かめてあります**（IP 判定 54 万件、公開サフィックス 33 万件、XML エスケープは全文字、SipHash 9 万件）。`SipHash` の値は DB に入っているので、**1 bit も変えていません** |
| **`UrlUtil.isIPUrl` が IPv6 の URL で true を返すようになった**（D-121） | `http://[::1]/` のホストは `URI.getHost()` が **`[::1]` と角かっこ付きで返す**ため、判定に渡す前に外しておらず、**IPv6 では一度も true になっていませんでした** |
| **`icu4j` を外した**（NF-L-01 / D-120） | `IcuUtil` の4つの変換（ひらがな⇄カタカナ、全角⇄半角）を自前の表にしました。**14.5MB の依存が 12KB のクラス1つになります**（実行時クラスパスの単独最大でした）。ICU が返していたものと **129 万通りを突き合わせて**あり、上の「ー」の件以外は 1 文字も変わりません（`IcuUtilTest`）。半角カナの取り込み、濁点の合成、`㋐` → `ア`、`㌀` → `アパート` もそのままです |
| **`juniversalchardet` を外した**（NF-L-01 / D-119） | 文字コードの判定を tika に寄せました。tika の判定器は CSV のために入れている `tika-parser-text-module` が連れてくるので、**依存は増えません**（Apache-2.0）。**245KB と MPL-1.1 / GPL-3.0 / LGPL-3.0 のトリプルライセンスが1本減ります。**94 ファイルで突き合わせて、**実寸のファイルでは答えが変わらない**ことを確かめてあります（`FileCharDetecterTest`）。**BOM は自前で読みます**（UTF-8 / UTF-16 / UTF-32）。tika の判定器は<b>クラスパスに jar が並ぶ順しだいで BOM を無視する</b>ためです（D-122）。ICU4J も測りましたが、短い日本語に弱いので採っていません |
| **ドキュメントの食い違い 3 件** | `execution.md`「送ったら止まる」で `after` も止まると書いていた（`after` と `onComplete` は `finally` にあるので必ず通る）／`config.md` の `migration.on_startup` の既定を `false` と書いていた（実際は `"auto"`）／`deploy.md` の起動ログ例が jar の外の conf を「クラスパスより優先」と書いていた（jimble はクラスパスしか見ない） |

### 足した

| | |
|---|---|
| **サンプル7本目：DB の込み入った話**（N-3 / D-141） | `examples/approval-data` を足しました（**データベースを2つ・参照用の接続・ぶら下げたサブ DB・トランザクション・キャッシュ・分散ロック・`DBValue`・コードマイグレーション・一括登録**）。**これで7本そろい、N-3 は完了です。**主題は<b>トランザクションで守れる範囲</b>で、承認（申請の状態＋通知）は同じ DB なので囲えますが、<b>監査ログは別のデータベースなので囲えません</b>——だから確定したあとに書きます。逆にすると「承認は戻ったのに承認したというログだけ残る」になります。<b>jimble は分散トランザクションをやりません</b>ので、2つの DB にまたがる以上どちらかは諦める、という形をそのまま置いてあります。「サブ DB」の2つの意味（**トップレベルをもう1本**と**親の `subs`**）も両方書いてあります——<b>後者にはマイグレーションも codegen も当たりません</b> |
| **サンプル6本目：画面を返す**（N-3 / D-140） | `examples/approval-pages` を足しました（**jte のレイアウト継承と部品化・静的配信・SPA・MPA・リバースプロキシ**）。<b>DB を使わない</b>ので `./gradlew :examples:approval-pages:run` だけで起動します。<b>`@template.` の実例はリポジトリ初</b>です——これまでの jte は9枚とも単独の完結 HTML で、レイアウトを共有していませんでした。リバースプロキシの転送先も同じプロセスの別ポートに立ててあるので、<b>`X-Forwarded-*` が足されて `Host` が張り替わる</b>ところが転送先の目から見えます |
| **サンプル5本目：時間のかかる仕事**（N-3 / D-139） | `examples/approval-jobs` を足しました（**バッチ・cron・キュー・リトライ・デッドレター・チャンクバッチ**）。<b>失敗する道を実際に通す最初のサンプル</b>です——`examples/blog` の MQ は必ず成功するので、リトライもデッドレターも一度も動いたことがありませんでした。積むときに `fail_until` を入れると<b>その回数まで落ちて、やり直されて、最後に `dead` になる</b>ところが目で見えます。`dead` になった行は<b>フレームワークが掃除も再投入もしない</b>ので、`GET /dead` と `POST /dead/retry` をアプリ側で1画面ぶん書いてあります。**入口が3つ**（Web / バッチ / スケジューラ）あり、どれも同じ `Bootstrap.load()` を通ります |
| **一定件数ずつ読んでまとめて書くバッチ**（`AbstractChunkBatch`。F-B-12 / D-137） | `reader()` で1件ずつ読み、`process()` で加工し、`chunkSize()` 件たまったら `write()` でまとめて書きます。**1かたまりが1トランザクション**で、書き終わるたびに確定します。100万件を1つのトランザクションで抱えません。途中で落ちたら<b>そのかたまりだけ戻して全体を失敗</b>にし、どこまで確定したかを実行履歴に残します。かたまりの切れ目で中断を見るので、長いバッチでも安全に止まります。進み具合（`batch_history.execute_info`）を **`batch.progress_seconds`（既定 5 秒）ごとに書き換える**ので、走っている最中に「いま何件目か」が管理画面から見えます。**既存の `AbstractBatch` は何も変わりません**（継承先を選べます）。読み方は `KeyPagingReader`（キー順のページング）を勧めます——カーソルは開いている間ずっとコネクションを1本押さえるためです。`batch.md`（日英）に節を足しました |
| **サンプル4本目：動いている中を見る**（N-3 / D-136） | `examples/approval-ops` を足しました（**ヘルスチェックとグレースフルシャットダウン・メトリクス・トレース・流量制限・Bot ブロック・内部呼び出し**）。<b>DB を使わない</b>ので `./gradlew :examples:approval-ops:run` だけで起動します。`BotBlocker` / `Metrics.snapshot()` のルート / `JimbleOtel.install` / `dispatcher().call(...)` / `rateLimit` は<b>アプリのコードとしては初の実例</b>です |
| **サンプル3本目：たくさんの中から探す**（N-3 / D-134） | `examples/approval-list` を足しました（**ページング・JOIN・絞り込み・集計・条件付き集計・CSV の書き出し・SQL 結果キャッシュ**）。`Paging` / `groupBy` / `ScopeCache` / `Column#urlPathPlaceholder` を<b>サンプルで初めて通しています</b>（どれも実例がありませんでした） |
| **サンプル2本目：入力を確かめる**（N-3 / D-133） | `examples/approval-forms` を足しました（**バリデーション・明細のネストパラメータ・下書きと提出で必須が変わる・添付・生成した型付きアクセサ**）。<b>生成された `AbstractXxxData` を実際に使う最初のサンプル</b>です（いままで生成はされていて、誰も使っていませんでした） |
| **機能ごとのサンプルを置き始めました**（N-3 / D-132） | `examples/approval-auth` を足しました（**ログイン・セッション・ルート属性による認可・CSRF・パスワード照合・Basic 認証**）。題材は「社内の申請・承認」で、以降のサンプルも同じ題材から機能ごとに切り出します（全7本の予定。`docs/design-n3.md`）。**1本ずつ単体で完結します**——テーブルもマイグレーションも設定も各アプリが自分で持つので、<b>1本開けば真似できます</b>。**PostgreSQL 単独**です（方言の両対応は `examples/blog` が引き続き担保します）。`examples/blog` はそのまま残ります |
| **アクセスログを切れるようになった**（NF-O-02 / D-130） | `server.access_log = false` で、1行も出さなくなります（**既定は `true` のままです**）。**1リクエストの割り当ての約4割がここ**（約 8,100 byte のうち約 3,400 byte。測るたびに数十 byte 動く）で、切ると秒あたりの本数が変わります（10 コアの Mac・8接続で 52,537 → 57,030 rps。<b>約 8%</b>）。**台によりますので、切る前に自分の台で測ってください**（`jimble-load/load.sh` が出す版と出さない版を並べて測ります）。行を組み立てる仕事も、ボット判定（User-Agent の照合）も止まります。**メトリクスとトレースは残ります。**<b>切ると、後から「あのとき何が起きたか」を調べる手段が無くなります</b>——500 が出ていたことも、誰がどのパスを叩いたかも残りません。前段（ロードバランサや nginx）が同じ内容を残していて、かつ実測して足りないと分かったときだけにしてください |
| **署名・暗号の鍵を、止めずに入れ替えられるようになった**（NF-S-09 / D-128） | 新しい鍵を `secret` に、いままでの鍵を `previous_secrets` に書きます。**書くのは常に `secret`、読むときだけ `previous_secrets` も試します。**<b>誰もログアウトしません。</b>古い鍵で読めたら<b>その場で新しい鍵で書き直す</b>ので、アクセスしてきた人から順に入れ替わります（`sid` と `csrf_token` は枠組みが書き直し、アプリが書いた Cookie は `cookies().isStale(名前)` で見て自分で書き直します——有効期限は書いた側にしか分からないためです）。**いつ古い鍵を捨ててよいかはメトリクスで分かります**（`cookie.stale_secret` / `session.stale_secret`。これが増えなくなったら捨てられます）。起動ログにも本数が出ます。<b>`cipher.key` と `hash.password.pepper` は対象外です</b>——作った値が DB に残り、書き直せるのが<b>ログイン成功時だけ</b>なので、休眠ユーザーがいる限り古い鍵を捨てられません（アプリ側の手順をドキュメントに載せました） |
| **鍵が無いことを起動時に言うようになった**（F-S-08 / D-128） | `cookie.secret` が空だと<b>署名が丸ごと効かない</b>のに、例外も出ず Cookie は普通に読み書きできるので気づけませんでした。`session.store = "cookie"` なのに `session.secret` が空のときも言います。**止めはしません**（手元で動かすだけのときに鍵を強制すると「とりあえず動かす」ができなくなるため）。`examples/blog` にも署名鍵を入れました |
| **MCP を標準入出力でも動かせるようになった**（F-MCP-12 / D-127） | `McpStdio.run(app, app.mcp().registry())` の1行です。HTTP を立てず、クライアントにプロセスを起こしてもらう形で会話します。**ポートは開きませんがルート表は組む**ので、すでにある API をそのまま出す `RouteTool` も `before` の認証も、HTTP のときとまったく同じに効きます。仕様は「**標準出力に MCP のメッセージ以外を書いてはならない**」と決めていて、ログが1行混ざるだけでクライアントは接続を切ります。そこで**本物の標準出力は `McpStdio` だけが持ち、`System.out` は標準エラーに差し替えます**——気をつけて避けるのではなく、書けなくします。出力は端末の文字コードに関係なく **UTF-8** です（`LANG` が決まっていないコンテナで、日本語が全部 `?` になって出るのを防ぐため） |
| **変わったことをクライアントに知らせられるようになった**（F-MCP-13 / D-127） | クライアントが `subscriptions/listen` で購読し、アプリは変わったところで `McpNotify.resourceUpdated("blog://posts/1")` と1行呼びます。**頼まれた URI にしか届きません。**HTTP なら SSE、stdio なら同じ標準出力に流れます。**流せるのはリソースの2つだけ**です（ツールとプロンプトは起動時に明示登録するので、動いているあいだに増えも減りもしません。「対応している」と答えて一生届かないほうが困るので、`toolsListChanged` は `acknowledged` に入れません） |
| **一覧が多いときにページに分かれるようになった**（F-MCP-14 / D-127） | `params.cursor` で受け、`result.nextCursor` で返します。**既定は 100 件**なので、それ以下しか登録していないアプリの応答は今までと変わりません（`mcp.page_size` で変えられます）。**読めないカーソルは `-32602` で断ります**——黙って先頭に戻すと、クライアントは同じページを永遠に読み続け、しかもエラーが1つも出ません |
| **`server/discover` に応えるようになった**（F-MCP-16 / D-127） | 仕様が「サーバーは実装しなければならない」と定めているのに**入っていませんでした**。話せる版と、そのサーバーが何を持っているかを返します。**この呼び出しにだけは版のヘッダが要りません**（版を知るための呼び出しに版を要求すると、初めて繋ぐクライアントは何もできません） |
| **分散トレーシングが使えるようになった**（NF-O-05 / D-126） | `jimble-otel` を依存に足して、起動時に `JimbleOtel.install("my-app", "http://localhost:4318")` と1行書くだけです。**HTTP リクエスト・SQL 1文・MQ の積みと処理・バッチ1回**に自動で区間が付き、入ってきた `traceparent` は引き継ぎ、jimble の HTTP クライアントから出るときも自動で付きます。**MQ も繋がります**——積んだリクエストと、何分もあとに別のプロセスで動いた処理が1本のトレースになります。ログには `trace_id` / `span_id` が入ります（**実行 ID の形は変えていません**）。送り先は OTLP（Collector / Jaeger / Grafana Tempo など） |
| **使わないアプリの容量は1 byte も増えません**（NF-L-01 / D-126） | `jimble-core` が持っているのは口（`Tracer` / `Span` / `Tracing`）だけで、OpenTelemetry の実体は `jimble-otel` にあります。登録していないあいだ、`Tracing.start(...)` の費用は**静的な変数を1つ読んで分岐するだけ**で、**1 回あたり 0 byte**です（ベンチマークと同じ測り方で確かめています）。1リクエストあたりの割り当ては 7,966 → 7,993 byte（+27 byte）に収まりました |
| **`jimble-otel` は 0.88MB**（NF-L-01 / D-126） | OpenTelemetry をそのまま入れると 4.2MB になります（既定の送信器 okhttp が okhttp 851KB ＋ okio 374KB ＋ **kotlin-stdlib 1.7MB** を連れてくるため）。**JDK の `HttpClient` を使う送信器（20KB）に差し替え**、トレースしか出さないので `sdk-metrics` と `sdk-logs` も外しました。削りすぎていないことは、**OTLP を受ける口を立てて実際に送り、届いた中身を見るテスト**で確かめています（コンパイルだけでは分かりません） |
| **メトリクスが取れるようになった**（NF-O-04 / D-124） | `Metrics.snapshot()` が、リクエスト数・レイテンシの分布・接続プールの使用数・MQ の件数をまとめて返します。**`/metrics` のルートは生やしません。**外に晒すかどうかは認証も公開範囲もアプリの都合なので、`get("/metrics", c -> c.response().json(Metrics.snapshot()))` と1行書いてください（ヘルスチェックを持たないのと同じ理由です）。**分布は固定バケット**なので、何件入れてもメモリは増えません（そのぶんパーセンタイルはバケットの境目までしか言えません）。**レイテンシの名前は `GET /posts/{id}` のようなルートの型**で、生のパスは使いません（`/aaa` `/aab` … と叩かれるだけで名前が無限に増えるため。名前は 1000 種類が上限で、超えたら1回だけ警告して捨てます）。**MQ の滞留数だけは自動で登録しません**（1回 SQL が要るので、`Metrics.snapshot()` が DB を触ることになる。**DB が詰まっているときに限ってメトリクスも取れなくなる**のを避けるため）。要るときは `Metrics.gauge("mq.notice.pending", () -> queue.pendingCount())` と書いてください |
| **ベンチマークの内訳が全部説明できるようになった**（NF-P-05 / D-131） | 1リクエスト 約 8,100 byte のうち<b>約 3,700 byte（45%）に名前がありませんでした</b>（部品を1つずつ別に測って並べていたので、合計と一致しない分が残っていました）。<b>段ごとに積んで隣との差を名前にする</b>形に変えたので、名前の無い残りは出ません：足場 504／Context の生成 1,168／ルーティング 455／ハンドラの枠 304／後始末 5,724。**あわせて、アクセスログの実測が 1,000 byte ほど安く出ていたのを直しました**（単体で `Log.access` を呼んで測っていたため、実行 ID・SQL 集計・ホスト情報が載っていませんでした）。<b>正しくは約 3,400 byte ＝ 1リクエストの4割</b>です。**`bench` がまれに理由なく赤くなるのも直しました**：ルート数の比較が byte のぴったり一致で、<b>552 が一度 556 になっただけで落ちて</b>いました（64 byte のすき間を入れています） |
| **ベンチマークを入れた**（NF-P-06 / D-125） | `./gradlew bench` で走ります。CI では毎回流れます。**落とすのは「1回あたりに割り当てた byte 数」だけで、時間では落としません。**共用ランナーの時間は走るたびに 20〜30% ぶれるので、要件にあった「ベースライン比 -10%」をそのまま実装すると**直していないのに赤くなる日**ができるためです（赤が信用されなくなるほうが害が大きい）。割り当て量は同じコードなら機械が変わっても同じ値になります。いまの実測は **1リクエスト 7,969 byte**（うちアクセスログが 2,264 byte）、**ルーティング1回 544 byte / 約 170ns**。時間も `build/bench/bench.txt` に記録します（CI の成果物として持ち帰るだけで、見て落とすことはしません） |
| **マッチ結果はキャッシュしないと決めた**（D-6） | 積み残していた宿題です。測ったところ、**ルートが 1000 本あっても 10 本と 1 byte も変わりません**でした（木を降りているので、ルート数に依らない）。1リクエスト全体の 7% で、DB を触るアプリでは誤差に沈みます。いっぽうキャッシュを持つと**鍵は利用者が送ってくる生のパス**になり、無限に増える表を抱えることになります。得るものが誤差で失うものがヒープなので、入れません |
| **マイグレーション SQL を製品ごとに分けられる**（F-G-20 / D-113） | ファイル名の接尾辞（`001_create_post.mysql.sql` / `001_create_post.postgresql.sql`）。**接尾辞の無いファイルはどちらでも流れる**ので、両製品で通る SQL は分けなくてよい。接尾辞は `db.<名前>.product` に書ける名前と同じ表で見る（`mariadb` は `mysql`）。飛ばしたファイルは起動ログに出る。`examples/blog` の DDL を両製品で書き、**`pgTest` に載せた**（D-112 の残り） |
| **ドキュメントサイトの反映を自動化した**（NF-D-01 / D-117） | **タグを打つと** GitHub Actions がサイトを作り、配る先（`jimble-document`）の `dist/` を置き換えて push します。Cloudflare はそこを見ているので、そのままデプロイされます。**push のたびには反映しません**（サイトだけが公開版より先に進むのを止めるため）。ドキュメントだけ直したときは Actions の画面から手で流せます。あわせて**サイトの原稿をリポジトリに入れました**（`.gitignore`）。それまで原稿が入っていなかったので、CI の「ドキュメントサイトの生成」は**0 ページのサイトを作って成功していました**（`site` タスクは 0 ページでも成功します）。ページ数の確認を CI と反映の両方に入れてあります |
| **CI を入れた**（D-108） | GitHub Actions。`build`（DB なし）／`db`（MariaDB + Redis）／`pg`（PostgreSQL + Redis）の3ジョブ。`dbTest` / `pgTest` / `codegenCheck`（F-G-14）/ `migrate → codegen → compileJava` の連鎖（NF-T-07）/ サンプルアプリの疎通（NF-T-06）/ ドキュメントサイトの生成（NF-D-03）が毎回回る。**標準ランナーだけ**で、larger runner は使わない |
| **待ち受けを始めてから止め方を預けていた**（D-110） | `JimbleServer` は `WebServer` を start したあとに `Shutdown.add(...)` していたので、**そのあいだに `jimbleRun` が止めに来ると何も止まらなかった**（古いアプリがポートを握ったまま残る）。start の前に預けるようにした。あわせて `stop()` は待ち受け前なら即座に抜ける（猶予のぶん黙って寝ないように） |
| **`pgTest` が PostgreSQL のつもりで MySQL に繋ぎに行っていた**（D-112） | `application.pgtest.conf` が無いモジュール（`examples/blog`）では `application.conf` に落ちるため。**手元は MySQL も立っているので通ってしまい**、CI の PostgreSQL だけのジョブで初めて落ちた。conf が無いモジュールは `pgTest` を飛ばす（`SKIPPED` としてログに出る） |
| **Gradle 9 でプラグインの検証が落ちていた**（D-111） | キャッシュの可否と入力の正規化の注釈が無かった。Gradle 8 は警告で流すが 9 はエラー。`enableStricterValidation` を入れて**手元でも 9 と同じ厳しさ**で見るようにし、あわせて javadoc の警告 14 件も潰した |
| **サンプルアプリが、まっさらな DB では動かなかった**（D-109） | `examples/blog` は MQ のテーブルをバッチの入口でしか作っていないのに、**キューに積むのは Web** だった。まっさらな DB で記事を登録すると「トランザクションのコミットに失敗しました」で 500。**いちどでもバッチを動かしたマシンでは動く**ので手元では気づけず、**CI を入れた初回に出た**。起動時に用意するものを `Bootstrap` 1か所にまとめ、3つの入口とテストがそれを呼ぶようにした。落とし穴のページにも足した |
| **Apache Tika を 3.3.2 → 4.0.0 に上げた**（D-115） | `Parser.parse` / `Detector.detect` が `TikaInputStream` を取るようになり、`Detector.detect` に `ParseContext` が増え、`Metadata` が `HttpHeaders` / `TIFF` を実装しなくなった（`Metadata.CONTENT_TYPE` → `HttpHeaders.CONTENT_TYPE`、`Metadata.IMAGE_WIDTH` → `TIFF.IMAGE_WIDTH`）。**種別判定の答えは1つも変わっていません**（同じテストを両方の版で流して確認。そのために `FileUtil` に初めてテストを付けました）。**依存が増えます**：`juniversalchardet`（MPL-1.1）が消え、`tika-encoding-detector-mojibuster` / `tika-ml-core` / `tika-parser-datauri-commons` と **commonmark**（tika-core 4 の既定出力が Markdown になったため）が入ります。実行時クラスパスで約 +0.5MB |
| **画像の幅と高さが取れるようになった**（D-116） | いままで**常に 0** でした（`AutoDetectParser` に画像を読む parser が無いため）。JDK の `ImageIO` でヘッダだけを読みます（PNG / JPEG / GIF / BMP / TIFF）。**WebP は `ImageIO` に無いのでコンテナを自前で読みます**（`VP8 ` / `VP8L` / `VP8X`）。画素は展開しないので大きな画像でもヒープを食いません。**HEIC / AVIF / SVG と動画は 0 のまま**です。`tika-parser-image-module` は +9 jar / 約 5.4MB（pdfbox 一式）になるので入れていません |
| **CSV の文字コードと区切り文字が取れるようになった**（D-116） | 種別を名前から決め直すときに**文字コードごと差し替えていました**。あわせて、`text/csv; charset=UTF-8` のようなパラメータ付きの種別が CSV として扱われなかったのと、拡張子の無い一時ファイル（アップロードの置き場所）が CSV として扱われなかったのも直しました |
| **依存の脆弱性を見る口を作った**（NF-S-07） | Gradle の依存グラフを GitHub に登録して Dependabot alerts に当てる。あわせて Dependabot で週1の更新 PR |
| **`UrlUtil.normalizeUrl`**（D-107） | **未エンコードの URL でも、エンコード済みの URL でも、同じ答えになる**（2回通しても変わらない）。ホストは punycode にする。移送してきた `fullUrlEncode` は<b>エンコード済みを二重にし、ホストをパーセントエンコードしていた</b>（DNS が引けない）。`urlToEncodeUrl` は<b>パスの空白を `+` にしていた</b>（`+` が空白なのはフォームの書式だけ）。どちらも呼ばれていないので消さず、javadoc から新しいほうへ案内している |
| **静的配信のルートを外から触れる**（D-70） | `AssetController` / `SpaController` / `MpaController` に `routes()`。`install` した子のルートに `AttributeKey`（認証の除外・流量制限）を付けられる |
| **`NOT IN` の条件を読めるようにした**（D-105） | `NotIn` に `operator()` / `conditionValue()`。`In` と対称になった。**`IN` とは別の演算子（`WhereTerm.NOT_IN`）**にしてある（`NOT IN (1,3)` は「それ以外の全部」なので、1 と 3 の行タグだけ消すと**古い値が返り続ける**）。SQL 結果キャッシュは従来どおりテーブルごと消す安全側のまま |
| **`codegen` が外したテーブルの名前を出す**（D-68） | `session` のように、jimble の管理テーブルとアプリの業務テーブルで名前がぶつかりうる。黙って外すと**自分のテーブルのクラスが生成されないことに気づけない** |

### 中の整理

| | |
|---|---|
| **負荷試験を置いた**（NF-P-08 / D-129） | `./jimble-load/load.sh` で、秒あたりの本数とレイテンシ（p50 / p90 / p99）を測れます。**素の helidon と並べて出す**ので、差がそのまま jimble の上乗せ分です。**依存は1つも増えていません**（Java で書いてあるので Mac でも Linux でも同じものが走ります）。`jimble-load` は<b>公開しません</b>。**CI では回しません**（D-125。共用ランナーの時間は 20〜30% ぶれるため） |
| **負荷試験を置いた**（NF-P-08 / D-129） | `./jimble-load/load.sh` で、秒あたりの本数とレイテンシ（p50 / p90 / p99）を測れます。**素の helidon と、アクセスログを切った jimble と並べて出す**ので、<b>helidon との差が jimble の上乗せ分、切った版との差がアクセスログ1行の代金</b>です（D-130）。**依存は1つも増えていません**（Java で書いてあるので Mac でも Linux でも同じものが走ります）。`jimble-load` は<b>公開しません</b>。**CI では回しません**（D-125。共用ランナーの時間は 20〜30% ぶれるため） |
| **ビルドの規約を `build-logic` へ移した**（D-13。**完了**） | root の `build.gradle.kts` が 709 行になり、うち 293 行が全モジュール共通の設定でした。`jimble.java-conventions` / `jimble.test-conventions` / `jimble.publish-conventions` の3つに分け、各モジュールの `plugins {}` に書くようにしています。**どのモジュールが何を使っているかが、そのモジュールを見れば分かります。**あわせて **junit の3行が 12 か所から消え**、`examples` には `-sources.jar` / `-javadoc.jar` が作られなくなりました（publish しないので要りません）。root は 369 行、`gradle-plugin` は 199 → 117 行になりました。**フレームワークのコードは1行も変えていません**（`build/central` に出るファイル 330 個と POM 13 個が移行前と 1 byte も変わらないことを確認しています） |
| **POM の必須項目を1か所にした**（NF-L-04 / D-13） | `licenses` / `developers` / `scm` の 25 行を本体と `gradle-plugin` に書き写していて、「直すときは両方直すこと」と注意書きを付けていました。`build-logic` を**別ビルド**（buildSrc ではなく）にしたので、別ビルドである `gradle-plugin` からも取り込めます。あわせて**版の既定値**も `gradle/libs.versions.toml` の `jimble` 1か所に寄せました（以前は3か所に同じ文字列がありました）。**developer から `organization` / `organizationUrl` を外しました**：Maven Central が求めているのは「連絡が付くこと」で所属ではありません（Central にある caffeine / jspecify / fastcsv も `id` / `name` / `email` だけです）。**すでに公開した 0.2.0 の POM は変わりません**（公開済みのものは書き換えられないので、次の版から） |
| **規約プラグインは Kotlin ではなく Java で書いた**（D-13 / D-22） | `plugins { kotlin-dsl }` の実体（`org.gradle.kotlin:gradle-kotlin-dsl-plugins`）は **Gradle の配布物に入っておらず、Plugin Portal からしか取れません**（Maven Central には `org.gradle.kotlin` というグループ自体がありません）。D-22 が「Plugin Portal への依存を持ち込まない」と決めていて、しかもその理由として D-13 を名指ししているので、`java-gradle-plugin`（Gradle 同梱）で書いています。`gradle-plugin` と同じ形なので、読み方も同じです |
| **移送してきたコードの警告を全部潰し、`-Werror` にした**（D-15。**完了**） | `jimble-util` は移送時点の警告を種別ごと（`unchecked` / `rawtypes` / `fallthrough` / `deprecation` / `dangling-doc-comments` / `this-escape` / `cast` / `overloads`）落としていて、**javadoc の doclint も切っていた**。**コンパイル警告 81 件と doclint 45 件を潰して、抑止をやめた。**以降は警告が出たらビルドが落ちる。消せないものは、消せない理由を書いた `@SuppressWarnings` をその場所に付けてある |
| **ヘルスチェックの要件を実態に合わせた**（NF-O-03 / D-106） | 要件は「標準で提供する」と書いてあったが、実装は無く、ドキュメントは「アプリが書いてください」だった。**グレースフルシャットダウンで「先にヘルスチェックだけ落とす」順番をアプリが決めるため**という理由を要件側に書いた |
| **jimble が作るテーブルの名前を1か所に**（D-68） | `io.jimble.db.FrameworkTables`。作る側も codegen の除外側も同じ定数を使う。`GeneratorConf.FRAMEWORK_TABLES` は廃止 |
| **「送信済みなら打ち切る」判定をエラー経路にも通した**（F-C-13） | `Dispatcher` から `isSent()` の直書きが無くなり、判定は `Stage` だけになった |
| 使われていない `Generator.isGenerationRequired` を削除 | スキーマが変わっても版は変わらないので、生成を飛ばす判定には使えない。鮮度は `codegenCheck` が生成物そのものを突き合わせて見る |
| 同名の `FileCharDetecter` が 2 パッケージにあったのを解消 | `io.jimble.util.io.FileCharDetecter` を残し、`io.jimble.util.charset.FileCharDetecter` を削除（後者はどこからも使われていなかった） |

---

## 0.2.0（2026-09-08）

**PostgreSQL に対応した版。**アプリのコードは1行も変えずに
`db.xxx.product = postgresql` だけで動く。

### 追加

| | |
|---|---|
| **PostgreSQL 対応**（F-D-30） | SQL ビルダーとフレームワークの内部テーブルが `db.xxx.product`（`mysql` / `mariadb` / `postgresql`）で切り替わる。**SQL ビルダーを使うアプリ側のコードは変更なし。**識別子の囲み・関数名・upsert・`INSERT IGNORE`・`INTERVAL`・JSON の取り出し・真偽値と JSON のバインド・採番値の取り出しを方言に寄せた。その製品に書けないものは<b>SQL を組み立てたところで `DialectException`</b> になる |
| **テーブル定義クラスの生成が PostgreSQL でも動く**（D-98） | `./gradlew codegen` が `pg_catalog` を読む。連番・既定値・コメント・一意キーを MySQL と同じ形に揃える |
| **SQL DSL の充実**（F-D-31） | 文字列（`lower` / `substring` / `replace` / `lpad` / `locate` …）、数値（`abs` / `mod` / `power` / `greatest` …）、日付（`year` … `weekOfYear` / `dateAdd` / `dateDiff` / `unixTimestamp` …）、条件と型変換（`coalesce` / `nullif` / `ifThenElse` / `cast` / `regexp`）、集約（`countDistinct` / `stddev` / `groupConcat`）、**ウィンドウ関数**（`rowNumber` / `rank` / `lag` / `over` ＋ `PARTITION BY` / `ORDER BY` / `ROWS BETWEEN`）。約 60 個。**両製品で同じ答えを返す** |
| **SQL 結果のキャッシュ**（F-D-28） | `selectCached` / `selectListCached` で結果をキャッシュし、**更新系 SQL が走ったら関係するキャッシュだけを消す**。判定に主キーと一意キーを使う。置き場は `sql_cache.store`（memory / redis / db）。**既定は無効**（`sql_cache.enabled = false`。切っているときは更新側の処理が1行も走らない） |
| **同じ `AsyncData` を面ごとに違う形で返す**（F-A-11） | `TableNest.ON` / `OFF` / `AS_IS`。管理画面はテーブルネスト、ショップはフラット、を<b>同じクラスのまま</b>出し分けられる。既定は `AS_IS` で既存の出力は変わらない |
| **実装済みの API を MCP からも出す**（F-W-27 / F-MCP-15） | `RouteTool` でルートをそのまま MCP のツールにする。`before` / `after` / エラーハンドラ / 流量制限が<b>同じように効く</b> |
| **レートリミット**（F-R-15） | ルートに宣言し、ディスパッチャが `before` より前に見る。トークンバケット。置き場は `rate_limit.store`（memory / redis / db） |
| **グレースフルシャットダウン**（F-X-05） | SIGTERM で「ヘルスチェックだけ落とす → 待つ → 新規を断つ → 処理中を待つ → 止める」 |

### 変更

- `db.xxx.product` が設定に増えた。**書かなければ `mysql`**（既存の設定はそのまま動く）
- `jimble-db` が PostgreSQL の JDBC ドライバを `runtimeOnly` で持つようになった。
  要らなければ `exclude(group = "org.postgresql", module = "postgresql")` で外せる
- `SelectBuilder` / `InsertBuilder` などの `sql()` に `sql(Dialect)` が増えた。
  引数なしの `sql()` は<b>主データソースの製品</b>に落ちる（既存の呼び出しはそのまま）
- `application.local.conf` の `include "application.conf"` が効くようになった

### 直したもの

- **テーブル定義クラスの型の取り違え。**MySQL の `SHOW FULL COLUMNS` は enum / set の
  <b>値そのもの</b>を型名に含めるため、`enum('serial','parallel')` が `int`、
  `enum('unreal','real')` が `double` になっていた。enum / set は `String` に倒す。
  **enum 列を持つアプリは、`codegen` をかけたあとの差分を一度見ること**
- 数の列に関数の既定値（`nextval(...)`、`NaN`）が付いていると、
  生成される Java が<b>コンパイルできない</b>ことがあった
- `selectListWithRowCount` が `ORDER` も `LIMIT` も無い SQL で例外になっていた
- データソースを開くときの例外を「データベースが無い」に潰していたため、
  <b>パスワード違いでもデータベースを作りにいっていた</b>

### 気をつけること

- **テーブル定義クラスの生成（`codegen`）は再実行を推奨。**上の型の取り違えの修正が入る
- PostgreSQL で使うときの制限（空間関数の座標の順、`greatest` / `least` の NULL、
  正規表現の方言など）は [DB を使う](https://jimble.io/ja/db) と
  [SQL DSL](https://jimble.io/ja/sql) にまとめてある

---

## 0.1.0（2026-09-07）

最初の公開。

- Web（Router / Request / Response / SSE / WebSocket / セッション / アセット / SPA / MPA）
- DB（SQL ビルダー / トランザクション / マイグレーション / コード生成 / キャッシュ / 分散ロック）
- バッチ / MQ / バッチ管理画面
- MCP サーバー
- `jimble new` / `migrate` / `codegen` / `jimbleRun`（Gradle プラグイン）
- `AsyncData` の先読み

**アノテーションと DI を使わない。**コードを上から辿れば処理が分かることを最優先にする。
