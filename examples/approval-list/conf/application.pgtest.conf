# pgTest 用（./gradlew :examples:approval-list:pgTest）
#
# もともと PostgreSQL なので、違いは接続先を環境変数で差し替えられることだけ。
# それでも置いているのは、pgTest が
# 「application.pgtest.conf があるモジュールだけ走る」作りだからである。

include "application.conf"
